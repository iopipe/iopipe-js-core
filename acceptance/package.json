{
  "name": "acceptance-test",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "build": "cp ../lib/iopipe.js ./iopipe.js",
    "deploy": "serverless deploy",
    "invoke": "sls invoke -f simpleSuccess",
    "local": "sls invoke local -f simpleSuccess",
    "all": "npm run build && npm run local && npm run deploy"
  }
}
